import { connect } from 'react-redux';
import MapWrapper from './MapWrapper';

const msp = (state, ownProps) => {
    return ({});
};

const mdp = (dispatch) => {
    return ({});
};

export default connect(msp, mdp)(MapWrapper);